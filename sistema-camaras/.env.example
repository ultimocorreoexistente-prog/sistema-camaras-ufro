# Ejemplo de variables de entorno para desarrollo local

# Base de datos (opcional, usa SQLite por defecto)
# DATABASE_URL=postgresql://usuario:password@localhost/sistema_camaras

# Clave secreta para sesiones
SECRET_KEY=cambia_esto_en_produccion

# Entorno
FLASK_ENV=development
FLASK_DEBUG=1

# Puerto (Railway lo asigna automáticamente)
PORT=5000
